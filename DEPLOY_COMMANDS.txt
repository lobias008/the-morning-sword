# Sequential deployment commands for the-morning-sword

cd "D:\OneDrive\Desktop\My agent"

git init
git branch -M main

gh repo create lobias008/the-morning-sword --public --source . --remote origin

git add package.json index.js README.md .gitignore DEPLOY_COMMANDS.txt
git commit -m "Initialize the-morning-sword Bitget Playbook agent"

git push -u origin main
